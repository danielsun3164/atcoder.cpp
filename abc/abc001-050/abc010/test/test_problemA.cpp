#include <bits/stdc++.h>
#include <command.h>
#include <gtest/gtest.h>

using namespace std;

static_block {
	COMMAND = "problemA";
}

TEST(abc010_problemA, case1) {
	check(string("") + "chokudai", string("") + "chokudaipp");
}

TEST(abc010_problemA, case2) {
	check(string("") + "sanagi", string("") + "sanagipp");
}
