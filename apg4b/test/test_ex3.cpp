#include <bits/stdc++.h>
#include <gtest/gtest.h>
#include <command.h>

using namespace std;

static const string COMMAND = "ex3";

TEST(apg4b_ex3, case1) {
	check(PATH + COMMAND, string(""), string("") + "5050");
}
