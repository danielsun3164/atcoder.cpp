#include <bits/stdc++.h>
#include <command.h>
#include <gtest/gtest.h>

using namespace std;

static_block {
	COMMAND = "problem006";
}

TEST(math_and_algorithm_problem006, case1) {
	check(string("") + "100", string("") + "203");
}

TEST(math_and_algorithm_problem006, case2) {
	check(string("") + "27", string("") + "57");
}
