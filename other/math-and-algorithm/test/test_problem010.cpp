#include <bits/stdc++.h>
#include <command.h>
#include <gtest/gtest.h>

using namespace std;

static_block {
	COMMAND = "problem010";
}

TEST(math_and_algorithm_problem010, case1) {
	check(string("") + "5", string("") + "120");
}
