#include <bits/stdc++.h>
#include <command.h>
#include <gtest/gtest.h>

using namespace std;

static_block {
	COMMAND = "problem059";
}

TEST(math_and_algorithm_problem059, case1) {
	check(string("") + "10", string("") + "4");
}
