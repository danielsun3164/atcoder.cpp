#include <bits/stdc++.h>
#include <command.h>
#include <gtest/gtest.h>

using namespace std;

static_block {
	COMMAND = "problem060";
}

TEST(math_and_algorithm_problem060, case1) {
	check(string("") + "4", string("") + "Second");
}
