#include <bits/stdc++.h>
#include <command.h>
#include <gtest/gtest.h>

using namespace std;

static_block {
	COMMAND = "problem066";
}

TEST(math_and_algorithm_problem066, case1) {
	check(string("") + "3 1", string("") + "24");
}
